# Stripe Configuration
STRIPE_SECRET_KEY=SOMEKEY
STRIPE_PUBLISHABLE_KEY=SOMEKEY
STRIPE_SUCCESS_URL=https://${PRODUCTION_DOMAIN}/checkout?status=success
STRIPE_CANCEL_URL=https://${PRODUCTION_DOMAIN}/checkout?status=cancelled

# Django Configuration
DEBUG=False
SECRET_KEY=SOMEKEY

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_FORWARDER=something@gmail.com
EMAIL_FORWARDER_PASSWORD=someAppPassword

# Admin Configuration
OWNER_EMAIL=owner@owner.com
OWNER_PASSWORD=SecureAdminPassword123!

# Production Domain (use localhost for development)
PRODUCTION_DOMAIN=example.com

# Public IP Address (for testing only - not used in production URLs)
PUBLIC_IP=40.233.106.142

# Frontend API URL (production uses nginx proxy, development can override)
VITE_API_URL=https://${PRODUCTION_DOMAIN}/api
VITE_PUBLIC_IP=${PUBLIC_IP}
