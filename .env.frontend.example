# Frontend Environment Configuration
# For deployment on public-facing machine

# Stripe Configuration (frontend only needs publishable key)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# SSL/Domain Configuration
PRODUCTION_DOMAIN=yourdomain.com
OWNER_EMAIL=admin@yourdomain.com

# Backend Configuration (private network)
# Can be IP address or DNS name (DNS name preferred)
# Examples: backend.internal, api-server.company.local, 10.0.1.100
BACKEND_HOST=backend.internal

# Certbot Configuration
CERTBOT_EMAIL=${OWNER_EMAIL}

# Note: No database, Django, or backend secrets needed on frontend machine